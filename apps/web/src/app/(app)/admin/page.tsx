/**
 * /admin — family admin hub (Task 15).
 * Inside authed (app) tree; non-admins get friendly empty state.
 */
"use client";

import { AdminPage } from "@/components/admin/AdminPage";

export default function AdminRoutePage() {
  return <AdminPage />;
}
